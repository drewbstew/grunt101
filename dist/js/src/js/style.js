(function() {
  var $searchBox, $searchButton, $searchDiv, setContainerHeight;

  $searchButton = $("#search-btn");

  $searchBox = $("" + (search - box));

  $searchDiv = $(".search");

  setContainerHeight = function() {};

  $(function() {
    return setContainerHeight();
  });

}).call(this);
